apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-base
spec:
  replicas: 1 
  selector:
    matchLabels:
      app: go-base
  template:
    metadata:
      labels:
        app: go-base
    spec:
      containers:
      - name: go-cont
        image: truong/go-base
        ports:
        - containerPort: 1234 
---
apiVersion: v1
kind: Service
metadata:
  name: go-base
spec:
  selector:
    app: go-base
  ports:
    - protocol: TCP
      port: 80
      targetPort: 1234